# External port mapping (host port that maps to container's port 3001)
# Change this to expose the application on a different port on your host
EXTERNAL_PORT=3001

# Database path (inside container)
DATABASE_PATH=/app/data/otpmanager.db

# JWT Secret - REQUIRED: Change this to a secure random string in production!
# Generate a secure secret with: openssl rand -base64 32
JWT_SECRET=changeme-please-use-a-secure-secret-key-in-production
